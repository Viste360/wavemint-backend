import express from "express";
const router = express.Router();

// placeholder
router.post("/", async (req, res) => {
  res.json({ status: "AI Smart Crop Placeholder" });
});

export default router;
